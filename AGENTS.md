## Objective
- Build GateVision (AI Vehicle Access Control System): completed auth + all feature centers. Both Recognition Center and Identity Management Center are now fully API-integrated (no mock imports).
- Phase: Performance Optimization & Production Readiness (Prompt 23) — freeze feature development; only stability/speed/cleanup improvements.

## Important Details
- React 19 + Vite + TypeScript + Tailwind v4 + shadcn/ui; npm registry `registry.npmmirror.com`
- `noUncheckedIndexedAccess: true`, `strict: true`; `noUnusedLocals: false`
- React 19 `useRef<T>()` no-arg fails -> use `useRef<T | null>(null)`
- All motion respects `prefers-reduced-motion` via `useReducedMotion()`
- Build: `.\node_modules\.bin\tsc --noEmit` (0 errors = silent), `.\node_modules\.bin\vite build` (chunk-size warning silenced via `manualChunks` vendor splitting - entry chunk now ~78 kB)
- **Tabs component is self-contained**: `src/components/ui/tabs.tsx` uses a `TabsContext` provider - `TabsTrigger` calls `onValueChange(value)` on click and `TabsContent` renders only the active panel (`active` prop wins, else context value match). Do not add radix-ui/react-tabs; every page using `Tabs` (System, Identity, Admin, Settings, profiles) relies on this implementation.
- **Write tool gotcha**: special Unicode chars (`...`, `---`) cause JSON parse failure - use ASCII only
- **Subagent gotcha**: subagents may overwrite existing foundation files (types, utils, mocks, hooks) with truncated versions - always verify foundation files survive subagent writes
- **TypeScript `noUncheckedIndexedAccess`** requires `!` on all array accesses (`arr[i]!`), optional chaining for object property access
- Badge component supports variants: `"default" | "success" | "warning" | "danger" | "info" | "neutral" | "outline"`
- Backend runs at `http://localhost:8000`, Vite proxies `/api` -> `http://localhost:8000`; API base URL from `VITE_API_BASE_URL` env var (`http://localhost:8000/api/v1`)
- JWT Bearer auth - token persisted to localStorage under `gatevision-auth`, auto-refreshed on 401 via refresh token
- All authentication is backend-driven; `@tanstack/react-query` for server state, Zustand for auth client state
- React Query hooks file `use-identity-api.ts` provides all identity API hooks; `use-identity.ts` is a convenience wrapper with search/filtering
- Backend: launch with `venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000` (run.py `reload=True` WatchFiles crashes silently on Windows - avoid)
- Backend AI model files (download via curl from GitHub releases into `models/` or `~/.EasyOCR/model/`): `models/yolov8n.pt` (plate detection), `~/.EasyOCR/model/craft_mlt_25k.pth` + `english_g2.pth` (OCR)
- Pipeline response contract (backend `/pipeline/process/upload`): `{success, message, data:{request_id, plates_detected, plates_recognized, recognized_plates[{plate,confidence,validation_status}], faces_detected, face_recognitions[], processing_time_ms, stage_results[{stage_name,success,duration_ms,error}], warnings, errors, decision?, gate_workflow_result?}}`; stage names prefixed `_` (e.g. `_detect_plates`, `_recognize_plates`, `_evaluate_decision`, `_process_gate_workflow`); decisions `GRANT|DENY|MANUAL_REVIEW`; detection bbox is 4-value `[x1,y1,x2,y2]`; `PlateReader` downscales oversized crops to `OCR_MAX_IMAGE_DIM` (480) before EasyOCR (CPU OCR is slow otherwise - ~35-65s for 6 crops on bus.jpg, and it blocks the event loop); first OCR run also loads EasyOCR models (~7s); pipeline requests use `timeout: 300_000`

## Work State
### Completed
- **Exit rejection now recorded as DENY in history + Live Gate narration cleanup (Prompt 27 follow-up)**: User reported Recognition History showed the rejected exit as "Granted" (60.1%) and saw a contradictory "face verified" then "face did not match" sequence. (1) Root cause of the history bug: `_evaluate_decision` runs before `_process_gate_workflow`, so the `DecisionRecord` was persisted with the engine's provisional GRANT and never reflected the gate's rejection. Fix: new `orchestrator._deny_on_gate_rejection` - when the gate service returns `success=False` for a GRANT decision (exit face mismatch, duplicate entry), it flips `context.decision` to DENY (so the API response + frontend agree), appends "Gate: <reason>" to the explanation, and updates the persisted record via new `DecisionRepository.update_decision(request_id, decision, explanation)` (find_one + save, no-op when missing, swallowed into a `decision_override` warning on Mongo-down). `recognition/routes.py::_history_item` needed no change (it already reads `record.decision`; DENY maps to "denied" via `_map_decision`). Frontend `buildFinaleSteps` now prioritizes the gate reason: for `granted`/`denied` + `gate.success===false` it speaks the gate `error` (e.g. "Face does not match the entry driver") with "already inside/outside" special-cases, falling back to "The gate could not be opened" (no-detail) or generic "Access denied". (2) Contradiction fix: `FACE_SCAN_STEPS` no longer claims "Verifying your identity..." (in session mode the face is only captured, not identity-verified) - now "Capturing your face..."; `buildFinaleSteps` removed the false "Face identity verified. Thank you." claim (face-detected now shows "Face captured."); `validateFace` no longer says "Now completing the verification..."; vehicle->face transition is "Now please look at the face camera." (dropped "so I can verify you"). (3) UX shortening: SCAN_STEPS 5->3, FACE_SCAN_STEPS 3->1, finale preamble steps removed ("All checks complete"/"Finalizing the verification"/two-step fingerprint collapse), `runJourneySteps` gap 650->400ms and finale gap 700->450ms, `speakLine` rate 0.92->1.05 + timeout words*480+1200 -> words*340+700 (faster TTS). Outcome badge "ENTRY BLOCKED" -> direction-neutral "GATE BLOCKED". Tests +4 (**752 passed**): `_deny_on_gate_rejection` downgrades decision + calls `update_decision` with reason, graceful warning when update fails (mongo-down), `update_decision` found + not-found; `test_session_mode_exit_face_mismatch_surfaces` now also asserts decision DENY + reason in explanation. tsc silent, vite build passes. NOTE: pre-existing GRANT history rows from before this fix are stale - use Clear History to remove them.
- **Exit face-match surfaced in UI (Prompt 27 follow-up)**: The user asked "why no face matched" on the Live Gate EXIT - root cause: the face-recognition stage matches the captured face against the **driver gallery** (`DriverProfileRepository`), which is empty until drivers are enrolled via `/face/enroll`, so `similarity_score`/`matched` are always null in session mode -> the face card showed "No match data". The real exit security check (exit face vs entry session's stored face) runs inside the gate workflow and its `face_score` was already in the API response (`gate_workflow_result.session.match`) but the frontend dropped it. Fix: `pipeline.api.ts` `mapGate` now extracts `session.match` into `gate.match` (`ApiGateWorkflow.match` added with `matched/score/plate_score/vehicle_score/face_score/face_mismatch/reason`); `normalizePipelineResult` overrides `face.similarity = round(face_score*100)` + sets `face.match_source = "session"` (new `ApiFaceData.match_source: "gallery" | "session"`, domain `FaceMatch.matchSource`); `mapFaceData` passes it through. `FacePanel` now shows "Session Driver / Matches entry driver" for session matches; Live Gate overlay face card shows `"NN% match with entry driver"` for session matches, the gate `error` when an exit is rejected (e.g. "Face does not match the entry driver"), falling back to `"NN% match"` (gallery) / "No match data". Live-verified against a real backend (entry car+astronaut -> exit car+astronaut): exit response carries `session.match.face_score` = 1.0 (identical images) which maps to a 100% session match. tsc silent, vite build passes.
- **Exit driver-consistency fix (security, Prompt 26 follow-up)**: On exit, the same car with a DIFFERENT face granted access because `ActiveSessionMatcher._is_match` accepted any candidate with an exact plate match regardless of face similarity (face_score was only consulted in the embedding-fallback path). Fix: `app/services/gate/active_session_matcher.py` now requires the exit face to match the entry session's stored face (`>= SESSION_MATCH_FACE_REQUIRE_THRESHOLD` 0.65, new setting, cosine via SimilarityEngine) even with an exact plate match - when BOTH the exit capture and the entry session have a face embedding; a low score flips the match to False with `face_mismatch=True` and reason "Face does not match the entry driver". `MatchResult` gained `face_mismatch: bool` (serialized in `to_dict`); the mismatch surfaces as `SessionGateError` -> `gate_workflow_result.success=false` + `error` and the entry session stays open. Degradation: no enforcement when entry stored no face or exit captured no face (plate-only match stands). Frontend: `buildFinaleSteps` gateRejected branch is reason-aware (shows `gate.error`; only claims "already inside/outside" when the error actually says so); `DecisionPanel` shows `gate.error ?? gate.message`. Tests +6 (**748 passed**): 4 matcher (different-face rejected, same-face accepted, no-exit-face plate fallback, no-entry-face skip), 1 gate service (SessionGateError propagation), 1 orchestrator (exit rejection error surfaces). Live smoke (phase C/D of `scripts/smoke_finalize.py`): entry car+astronaut -> exit same car + zidane = REJECTED ("Face does not match the entry driver", session stays INSIDE, verified via `smoke_debug.txt` session state); exit same car + astronaut = SUCCEEDS and session closes. tsc silent, vite build passes.
- **Two-phase Live Gate flow - vehicle scan then face scan (Prompt 26)**: Live Gate (`live-gate-overlay.tsx`) now runs a true two-step identity check when `requireFace` is on. Phase A scans the vehicle (plates + fingerprint, `require_face=false`); when the vehicle passes, it transitions to a dedicated `face_scan` phase with a live browser-camera view + dashed oval "Position your face here" guide and an "Upload a Photo of My Face" fallback. The face photo is validated first via the existing `POST /face/recognize/upload` (`recognizeFaceUploadApi`) with retry guidance on no-face/failure; after `MAX_FACE_ATTEMPTS` (3) the flow ends in a DENY finale ("Access denied. We could not verify your identity...") rather than looping on the vehicle camera (the old `face_prompt` phase just re-ran the vehicle scan - now removed). On face-detected success, a combined submission runs with a new optional `face_file` on `POST /pipeline/process/upload`: backend `pipeline/routes.py` passes `face_data` into `orchestrator.execute_from_upload`, which decodes it into a new `PipelineContext.face_frame` (plus `face_frame_metadata`); `_recognize_faces` uses `face_frame` when present (else falls back to `frame`), so plates/vehicle fingerprint come from the vehicle image while face recognition runs on the dedicated face photo; a single pipeline run + single gate workflow produces the final decision. Frontend: `processPipelineUploadApi(file, direction, onProgress, requireFace, faceFile?)` appends `face_file` to the FormData; `useProcessPipeline` accepts `faceFile`; the overlay keeps the vehicle `File` in `vehicleFileRef` for the combined call, adds `facePreviewUrl`/`faceDenied`/`scanKind` state, a stream-reattach effect (fixes black video when swapping video/img/video), face-safe file-input routing, and error-retry returns to `face_scan` instead of restarting the vehicle. Suite **739 passed** (+3: separate face frame used for recognition, fallback to vehicle frame, invalid face bytes 422 `ContextValidationError`), tsc silent, vite build passes.
- **Face-match decision bug (Prompt 26 follow-up)**: Live Gate combined run returned MANUAL_REVIEW/ENTRY BLOCKED even with a valid plate + captured face. Root cause: `orchestrator.py::_build_face_recognitions` built decision-engine stubs WITHOUT `detections`, so `SessionVerificationService.verify_from_pipeline` never saw the face embedding -> `face_not_captured` -> with `require_face=true` always MANUAL_REVIEW. Fixed by passing `detections` (embedding + confidence) through in `_build_face_recognitions`; the API response schema (`schemas/pipeline.py::FaceRecognitionResult`) whitelists fields so 512-dim embeddings are NOT leaked to the frontend. Added regression test `test_pipeline_require_face_grants_when_face_captured` (valid plate + face + `require_face=true` -> GRANT, `session_verification.face_captured` true; uses wide plate bbox `[10,20,60,35]` to pass `PLATE_MIN_ASPECT_RATIO` 1.5 crop filter, real `DecisionEngine`, `require_face` passes through `execute_from_upload`). Suite now **740 passed**. Note: phase-A vehicle run still opens the gate/creates the entry session before the combined run, so a follow-up combined run on the same vehicle yields the "already inside" gate message - consider gating phase-A's gate workflow (or merging into one run) later. Frontend also: `live-gate-overlay.tsx` camera errors now explain the exact cause (non-secure context, permission denied, no camera, in use), both vehicle + face views got an "Enable Camera" retry button, and `startCamera` auto-retries 4x/800ms on transient busy. tsc silent.
- **Two-phase flow finalization fix (Prompt 26 follow-up)**: Live flow produced TWO granted history records and a "vehicle already INSIDE" gate rejection, because phase A (vehicle-only, `require_face=false`) ran the FULL pipeline including `_persist_decision` + `_process_gate_workflow` - it opened the gate and created the entry session, then the phase-B combined run (car+face) hit the duplicate-entry guard. Fix: new `finalize` flag (default `true`) on `PipelineContext` + `execute_from_upload`/`execute_from_camera` + `POST /pipeline/process/upload?finalize=` + `POST /pipeline/process/camera?finalize=`; `_persist_decision` and `_process_gate_workflow` no-op when `finalize=false`, so a pre-check run returns a decision without touching Mongo/gate. Frontend: `processPipelineUploadApi(file, direction, onProgress, requireFace, faceFile?, finalize?)` + `useProcessPipeline` accepts `finalize`; overlay's vehicle-only run sends `finalize:false` (no gate/history) and only the combined car+face run finalizes -> exactly ONE grant + ONE entry session. Also: `EvidenceCollector._build_face_evidence` was passing the full face_recognitions dict into `decision.evidence[].metadata` (512-dim embeddings leaked to the API response after `_build_face_recognitions` gained `detections`) - now strips `detections` (keeps `detection_count`). Narration: `FACE_SCAN_STEPS` no longer says "Matching your face against the vehicle record" (there is no driver gallery to match against in session mode) - now "Capturing your face for the record". Added `test_persist_decision_skipped_when_not_finalize` + `test_gate_workflow_skipped_when_not_finalize`. Suite now **742 passed**, tsc silent, vite build passes. Live-verified end-to-end via `scripts/smoke_finalize.py` + `scripts/run_smoke_server.py` (spins uvicorn in a thread on port 8001 with `DATABASE_NAME=gatevision_smoke`, drives the real `/pipeline/process/upload`): phase A (synthetic APP971KS plate frame, `finalize=false`) -> GRANT with NO gate workflow and zero persisted records; phase B (car + astronaut face, `finalize=true`) -> GRANT with gate workflow and EXACTLY ONE decision + ONE gate session + ONE transaction; repeat pre-check stays side-effect-free; no 512-dim embeddings leak from face evidence. NOTE: background uvicorn processes get console-killed (0xC000013A) in this environment when spawned from the shell - always run the smoke via `python scripts/run_smoke_server.py` (foreground, in-thread server). The user's running backend on port 8000 predates the `finalize` change and must be restarted before browser-verifying the two-phase Live Gate flow.
- **Face enrollment + live gallery matching wired end-to-end (Prompt 25)**: (1) `scripts/face_smoke_test.py` proves the real InsightFace pipeline works live (astronaut: 1 face @0.89 conf; insightface t1: 6 faces; ultralytics bus: 2 faces - the people by the bus; self-similarity 1.0 vs cross 0.163, all 512-dim, CPU ~2-10s/image). (2) Root cause of never-matching faces: orchestrator called `recognize_from_image(frame)` with NO `reference_embedding`, so the service's `similarity_score`/`matched` were always null/False. Rewrote `_recognize_faces` in `app/services/ai/orchestrator/orchestrator.py` with `_match_face_to_gallery`: captures embeddings -> `DriverProfileRepository.find_active()` gallery (drivers with `face_embedding_reference`) -> cosine similarity via `SimilarityEngine` -> best match above `settings.FACE_SIMILARITY_THRESHOLD` (0.65) populates `similarity_score`/`matched`/`matched_driver_id`/`matched_driver_name`/`embedding_distance`; Mongo-down degrades to no-match + warning, logged `face_matched`/`face_gallery_unavailable`. (3) `schemas/pipeline.py::FaceRecognitionResult` + `pipeline/routes.py` `_to_api_response` + orchestrator `_build_face_recognitions` all pass the 3 new driver fields. (4) Fixed `admin/enrollment_service.py` bug: read `embeddings` key that never exists (recognition returns `detections[*].embedding`), so every enrollment raised "Failed to extract face embedding" - now reads `detections`. (5) New `POST /api/v1/face/enroll` (multipart: file + driver_id + full_name + optional email/phone/department) via `EnrollmentService.enroll_driver_with_image`, returns `{driver_id, full_name, face_embedding_dimension}`. (6) Frontend: `pipeline.api.ts` `RawFaceRecognition` + normalizer pass driver fields, and face `similarity` is now scaled 0-1 -> 0-100 percent (fixed the gauge/overlay double-scale bug: `live-gate-overlay.tsx` dropped its `* 100`, `FacePanel` `>= 70` now works); new `services/api/face.api.ts` (enroll/recognize/compare/model-info), `lib/api/endpoints.ts` FACE section, `query-client.ts` FACE keys, `features/recognition/hooks/use-face-api.ts` (`useEnrollFace` mutation with success/error toasts + driver invalidations, `useFaceModelInfo`); `FaceEnrollDialog` (driver select + photo preview) wired into Identity page Drivers tab "Enroll Face" button. Suite **736 passed** (+3: face enroll API x2, orchestrator gallery-match test; `test_pipeline_face_integration` matched assertion updated to gallery semantics - without a gallery no match; `test_enrollment_service` mock updated to real detections contract). tsc silent, vite build passes. Note: identity router (including `/identity/enroll/*`) stays gated behind `ENABLE_ENTERPRISE_IDENTITY=false`; the new `/face/enroll` + pipeline gallery matching are always on and are what make Mode A face verification work.
- **Recognition History direction column (Prompt 25 follow-up)**: history table showed no entry vs exit. Root cause: direction was only stored on `GateTransaction.action` (GRANT runs); DENY/MANUAL_REVIEW records lost it. Added `direction` (default "entry") to `DecisionRecord` model + persisted via `context.direction` in `_persist_decision`; `recognition/routes.py::_history_item` now returns `direction` (from `txn.action` when a transaction exists, else `record.direction`) and includes it in the search haystack. Frontend: `direction: "entry"|"exit"` on `ApiRecognitionHistoryEntry` + `RecognitionHistoryEntry` + `mapHistoryEntry`, and `HistoryTable` gained a Direction column (arrow icon chip, entry green / exit blue) + "All Directions / Entry / Exit" filter toggle. Suite **736 passed**, tsc silent, vite build passes.
- **UI bug fixes + bundle split (Prompt 24)**: (1) **Tabs broken app-wide** - the custom `Tabs` component never wired trigger clicks to `onValueChange` and `TabsContent` always rendered every panel, so clicking tabs (System/Identity/Admin/Settings/profiles) did nothing. Rewired `components/ui/tabs.tsx` with a `TabsContext` provider: `TabsTrigger` calls `onValueChange(value)` on click, `TabsContent` returns null when inactive (`active` prop wins, else context value match). (2) **Presentation Mode had no exit UI** - the only toggle lived in `TopNav`, which is hidden while `isPresentationActive`; added a floating "Exit Presentation" button in `app-layout.tsx` (store `exit()`). (3) **Recognition History had no delete/clear/DB-removal** - added backend `DELETE /recognition/history/{record_id}` (removes DecisionRecord + linked GateTransaction + its GateSession only when no other txn references it) and `DELETE /recognition/history` (wipes all DecisionRecords + GateTransactions + GateSessions, returns deleted counts); added `delete_by_id`/`delete_all` to DecisionRepository/GateTransactionRepository/GateSessionRepository; frontend `deleteRecognitionHistoryEntryApi`/`clearRecognitionHistoryApi` + `useDeleteHistoryEntry`/`useClearHistory` mutations (invalidate recognition history + gate transaction/statistics/active keys), `HistoryTable` gained per-row trash button + two-step "Clear History" confirm, recognition page wires success/error toasts. (4) **Chunk-size warning silenced** - added `manualChunks` vendor splitting to `vite.config.ts` (react/tanstack/framer-motion/radix/lucide/axios/zustand/zod/forms/misc): entry chunk 520kB -> 78kB, no chunk >500kB, no warning. (5) **Unused dep removed** - `recharts` (zero imports in `src/`) dropped from `package.json` + lockfile (36 packages pruned) and the dead `vendor-charts` branch removed from `vite.config.ts`. Suite now **733 passed** (was 701; +5 recognition delete/clear tests). tsc silent, vite build passes.
- **Recognition page Input merge**: `UploadZone` (drop zone w/ inline thumbnail) and `InputPreview` ("Captured frame" preview) were two stacked sections showing the same image; merged into one `CaptureInput` component (`components/capture-input.tsx`) that swaps drop-zone <-> full preview (overlay/zoom/metadata + Replace Image/Clear actions). Removed the now-redundant header Clear button + dead `cleanPreview` callback; `readOnly` prop hides actions during playback. tsc silent, vite build passes.
- **Live Gate error-screen bug + Demo Center removal**: (1) Live Gate overlay showed a false "Sorry, I could not process that capture" error even when the pipeline API returned 200/GRANT. Root cause: `playGateSound` (WebAudio) was the only unguarded sync call in the scan flow and threw (`new AudioContext()` NotSupportedError / InvalidStateError on a blocked/suspended context), aborting the finale -> generic error. Fixed in `features/recognition/utils/sounds.ts` (getCtx try/catch, resume() rejection swallowed, playGateSound fully guarded) + `live-gate-overlay.tsx` (per-step sound try/catch so narration/reveal always continue, handleError surfaces real message from objects, runScan logs the abort). Verified `normalizePipelineResult`/`mapPipelineResult` against the real duplicate-entry response in Node - both provably safe. (2) Removed the entire Demo Center per user request: deleted `features/demo/`, `store/demo-store.ts`, `routes/demo.tsx`; un-wired `demoRoute` from `router.ts`, `AiStoryMode` from `app-layout.tsx`, Demo button + route title + store hook from `top-nav.tsx`, and the empty "Advanced" sidebar section. tsc silent, `vite build` passes (main chunk 569kB -> 522kB).
- **Reports & Analytics API contract fix (Prompt 23b)**: Fixed the Reports page "Failed to load reports data" error. Root cause: `reports/api/mapper.ts` `mapAnalyticsSummary` called `.map()` on backend `/admin/analytics` `decision_breakdown`, which is an **object** (`{total, grants, denials, manual_reviews, grant_rate, denial_rate, review_rate}`), not an array -> runtime throw -> `useApiAnalytics` query fails -> `isError` banner. Rewrote the whole reports data layer against verified backend contracts: `reports/api/types.ts` now mirrors real shapes (`hourly_traffic` hour/count, singular `daily_trend` with `_id`, object `decision_breakdown`, `processing_times`, `top_denied_vehicles`, `results`-based `ApiReportData`/`ApiManualReview`/`ApiEventItem`/`ApiSearchItem`/`ApiDecisionHistoryRecord`); `mapper.ts` converts object breakdown -> granted/denied/manual_review array and `hour: number` -> "04:00" label (reuses `formatHour`), empty typed fallbacks for gateComparison/vehicleDistribution/manualReviewTrend (typed arrays, not `[]` which infers `never[]` and breaks `useReports` gateUtil). `reports.api.ts` rewritten: `getReportsApi` builds ONE `ApiReportRecord` from transaction `results` (columns time/vehicle/driver/action/decision/gate/request_id), translates `page` -> `skip/limit` with flat query params (fixes the `params%5Bpage%5D=1` double-wrap bug); `searchReportsApi` maps `{type,data}` -> `ApiSearchResult` (transaction->transaction, decision->request); `getManualReviewsApi` maps reviews to pending/resolved/escalated; `getEventsApi` maps event dicts to `ApiEventSummary`. `event.api.ts` `getDecisionHistoryApi` now consumes `{results,total,skip,limit}` -> `PaginatedResponse` with GRANT/DENY/MANUAL_REVIEW -> granted/denied/manual_review. `export.api.ts` downloads real file via `api.get(..., {responseType:"blob"})` + object URL. `tsc --noEmit` silent, `vite build` passes (chunk-size warning only). Backend contracts double-checked in `admin/routes.py`, `reporting_service.py`, `analytics_service.py`, `audit_service.py`, `decision/routes.py`.
- **Performance & Production Readiness (Prompt 23)**: Backend per-stage timing + request-id logging middleware (`app/middleware/request_logging.py`, `X-Request-ID`, `request_start/completed/failed` logs, registered between SecurityHeaders and RateLimit); CPU-bound OCR/detection moved off the event loop via `asyncio.to_thread` (`ocr_service`, `detection_service`); shared-reader locks serializing EasyOCR `readtext` and YOLO inference (`plate_reader._lock`, `detector._lock`); camera capture throttle (`CAMERA_MIN_CAPTURE_INTERVAL_MS=1000`) + duplicate-frame guard (`is_duplicate_of_processed`/`note_processed`, raises friendly `ContextValidationError`); orchestrator `execute_from_upload/execute_from_camera` accept `request_id`; warmup structured logs; `OcrService._save_result` Mongo-down fallback returns `SimpleNamespace` (fixes `CollectionWasNotInitialized` crash). New `tests/test_pipeline_reliability.py` (13 graceful-failure tests: empty/corrupt upload, no plate, unreadable plate, multiple vehicles, face hidden, face failure, camera disconnected, OCR failure, persistence failure), `tests/test_request_logging_middleware.py` (4), camera throttle/dedupe tests, empty+corrupt 422 API tests. Suite now **701 passed**. `scripts/benchmark_pipeline.py` rewritten (synthetic 100 entry/100 exit dataset, seed 7, ~8% blur + ~8% no_plate, recognition/decision accuracy, per-stage timings, `reports/pipeline_benchmark.json` + `.md`, smoke-verified avg ~535ms with `_detect_plates` dominant). Frontend: new `ProcessingStatus` banner (humanized "current action" hint + elapsed timer) wired into Recognition page; removed all ineffective dynamic imports in `recognition/page.tsx` (static imports for `getPipelineStatusApi`/`getRecognitionResultApi`/`mapPipelineResult`). Dead-code sweep removed 40 orphaned files (verified via reverse-import graph): `services/api/{monitoring,user,health,camera}.api.ts`, `services/api.ts`, `services/mock/weather.service.ts`, 6 orphaned dashboard components + `use-dashboard.ts`, `recognition/hooks/{use-camera,use-recognition}.ts`, `routes/auth.tsx` + `layouts/auth-layout.tsx`, `styles/typography.ts`, `components/charts/`, `ui/{checkbox,command-palette,metric-card,popover}.tsx`, `layout/{app-logo,breadcrumb}.tsx`, `forms/{form-field,file-upload}.tsx`, 4 dead `hooks/*`, and dead barrels (`ui/layout/forms/brand/hooks/layouts/features/auth*/index.ts`). tsc silent, vite build passes (chunk-size warning only).
- **Auth (Phase 1)**: full mock auth/session/store/hooks/components/pages; CommandPalette, NotificationCenter, GlobalSearch, KeyboardShortcuts
- **Dashboard (Prompt 3)**: 14 components, build passes
- **Auth bug fixes**: GuestRoute login redirect, ActivityFeed `manual_review` union, session-persist on every login
- **Gate Operations (Prompt 6)**: 12 components + page, route `/gate-operations`, builds pass
- **Reports & Analytics (Prompt 7)**: 18 components + page, route `/reports`, builds pass
- **System Monitoring & AI Ops Center API Integration**: Fully API-driven. `api/types.ts` (12 backend-mirror interfaces: ApiSystemHealth, ApiModelHealth, ApiDatabaseHealth, ApiStorageInfo, ApiPerformanceMetrics, ApiConfigurationItem, ApiVersionInformation, ApiBackupRecord, ApiLogStatistics, ApiCleanupResult, ApiMonitoringStatus, ApiSystemAlert), `api/mapper.ts` (10 mappers: mapOverallHealth, mapAiModels, mapPerfMetrics, mapStorageInfo, mapConfigItems, mapVersionInfo, mapBackupRecords, mapCleanupResult, mapLogEntries, mapAlerts). 3 API services (`system.api.ts` with 9 endpoints, `backup.api.ts` with 3 endpoints, `monitoring.api.ts` with log stats). Updated `endpoints.ts` (SYSTEM section with 10 endpoints), `query-client.ts` (SYSTEM keys with 9 keys). `hooks/use-system-api.ts` (9 React Query hooks with 30s polling + 3 mutations). Rewritten `hooks/use-system.ts` (composite hook uses real API via React Query + mapper, derived static data for cameras/pipeline/topology/security, live log simulation preserved). `page.tsx` updated with loading skeleton + error/retry states. `DigitalTwinMonitor` hackathon (live topology graph with animated nodes, particle data flows, interactive node inspection panel, 24h health timeline SVG chart, Predictive Capacity panel with disk exhaustion forecast/throughput/suggested actions). Zero active mock imports (`mocks/data.ts` dead file remains). `tsc --noEmit` silent, `vite build` passes (chunk-size warning only).
- **Administration & Security Center**: types, utils, mocks, hooks, 20 components, page with 10-tab workspace, route `/admin`. Builds pass.
- **Settings & Configuration Center**: types, utils, mocks, hooks, 22 component files, page with sidebar + 14 workspace tabs + 8 modal dialogs, AI Configuration Simulator hackathon feature. Route `/settings`. Builds pass.
- **API Integration Layer (base)**: `src/types/api.ts` (ApiResponse, LoginRequest, LoginResponseData, HealthResponse, MeResponse, NormalizedError); `src/lib/api/` (axios.ts, interceptors.ts with JWT auto-attach and 401 auto-refresh, errors.ts, endpoints.ts, query-client.ts with QueryClient + QUERY_KEYS, api-client.ts typed wrapper); `src/services/api/` (auth.api.ts, health.api.ts, user.api.ts, recognition/*.api.ts x7, identity/*.api.ts x5, gate-session.api.ts, gate-transaction.api.ts, workflow.api.ts); App.tsx updated with ApiErrorBoundary; BackendStatus component with health check query. Builds pass.
- **Recognition Center API Integration**: Fully API-driven. `src/features/recognition/types/api.ts` (24 backend-mirror interfaces), `api/mapper.ts` (converts API->UI), 7 API service files, 2 React Query hooks (`use-recognition-api.ts`, `use-camera.ts`), updated `use-pipeline.ts`/`use-recognition.ts`, rewrote `page.tsx` (file upload -> backend pipeline -> polling -> result), updated all components to use API, built Investigation Timeline hackathon. Zero mock imports. Builds pass.
- **Gate Operations API Integration**: Fully API-driven. `api/types.ts` (backend-mirror request/response interfaces: ApiGateActive, ApiGateStatistics, ApiGateSession, ApiGateTransaction, ApiWorkflowResult, ApiMovementHistory, ApiActiveVehicle, ApiEntryRequest, ApiExitRequest, ApiSessionState), `api/mapper.ts` (5 mappers: mapSession, mapTransaction, mapActiveVehicle, mapMovement, mapMovementHistory). 3 API service files (gate-session.api.ts, gate-transaction.api.ts, workflow.api.ts). `hooks/use-gate-operations-api.ts` (8 React Query hooks: useActiveSessions 5s polling, useTransactions 10s, useGateStatistics 15s, useVehicleHistory, usePipelineStatus 5s, usePipelineMetrics 5s, useSystemHealth 30s, useEntryMutation, useExitMutation). Rewritten `hooks/use-gate-operations.ts` (composite hook uses real API, same GateOperationsApi interface, all 12 components work unchanged). `constants.ts` for static SiteMap layout (replaces mock import). `TrafficPlayback` hackathon with play/pause/rewind/speed/skip/timeline scrubber, stats bar, animated event list. Zero mock imports in gate-operations feature. Builds pass (tsc silent, vite build succeeds).
- **Reports & Analytics API Integration**: Fully API-driven. `api/types.ts` (12 backend-mirror interfaces: ApiReportRecord, ApiAnalyticsSummary, ApiHourlyTraffic, ApiDailyTrendData, ApiDecisionBreakdownData, ApiProcessingMetrics, ApiDenialTrend, ApiRecognitionStatistics, ApiPeakHourData, ApiGateComparison, ApiVehicleDistribution, ApiManualReviewTrend, ApiSearchResult, ApiManualReviewSummary, ApiEventSummary, ApiDecisionHistoryItem), `api/mapper.ts` (8 mappers: mapReportRecord, mapAnalyticsSummary, mapSearchResult, mapManualReviewSummary, mapEventSummary, mapDecisionHistoryItem + sub-mappers). 3 API service files (reports.api.ts, export.api.ts + updated analytics.api.ts). Updated `endpoints.ts` (ADMIN section), `query-client.ts` (REPORTS keys). `hooks/use-reports-api.ts` (8 React Query hooks: useApiReports, useApiAnalytics 30s, useApiSearch debounced, useApiManualReviews, useApiEvents 30s, useApiDecisionHistory 30s, useApiGateStatistics 30s, useApiExportMutation). Rewritten `hooks/use-reports.ts` (composite hook uses real API, computes derived KPIs from analytics data, same ReportsApi interface, 30+ data properties). 19 new components (traffic-hourly-chart, daily-trend-chart, decision-breakdown-chart, recognition-metrics, pipeline-stages, gate-utilization-chart, heatmap-grid, queue-wait-chart, models-status, security-insights-panel, risk-score-card, report-table, search-widget, export-widget, events-feed, decision-history-table, manual-review-queue, insights-feed, forecast-chart). Rewritten `page.tsx` with 11 sections + loading/error/retry states. `SecurityIntelligenceCenter` hackathon with 6-tab SOC analytics dashboard (Executive Summary, Security Timeline with risk assessment, Threat Heatmap 7x24 grid, AI Confidence Analytics with animated bars, Failure Explorer with deny/review stats, What-If Decision Weight Simulator with sliders + instant simulation). Zero mock imports in reports feature. Builds pass (tsc silent, vite build succeeds, chunk-size warning only).
- **Identity Management Center API Integration**:
  - `types/api.ts`: backend-mirror interfaces for DriverProfile, VehicleProfile, AccessPolicy, BiometricInfo, Timeline, Documents, Relationships, Stats, Enrollment, Verification
  - `api/mapper.ts`: converts API snake_case -> UI camelCase types
  - `services/api/`: identity.api.ts, driver.api.ts, vehicle-profile.api.ts, policy.api.ts, enrollment.api.ts (5 files)
  - `hooks/use-identity-api.ts`: 18 React Query hooks (useDrivers, useVehicles, usePolicies, useIdentityStats, useIdentityActivity, useIdentityRelationships, useCreateDriver, useCreateVehicle, useEnrollDriver, useEnrollVehicle, useVerifyIdentity, useUpdateDriver, useUpdateVehicle, useUpdatePolicy, useDeleteDriver, useDeleteVehicle, useDeletePolicy, useDuplicatePolicy)
  - `hooks/use-identity.ts`: rewritten to use React Query hooks internally; same return interface; no mock imports
  - `page.tsx`: removed MOCK_RELATIONSHIPS import, uses `useIdentityRelationships()` hook; added Identity Intelligence Panel as 6th tab
  - `driver-wizard.tsx`: accepts `vehicles` and `policies` as props (no more MOCK_VEHICLES/MOCK_POLICIES imports)
  - `vehicle-wizard.tsx`: accepts `drivers` and `policies` as props (no more MOCK_DRIVERS/MOCK_POLICIES imports)
  - `components/identity-intelligence-panel.tsx`: hackathon feature with live threat signals, interactive identity radar, biometric health, AI confidence score, animated metrics
  - `query-client.ts`: IDENTITY keys added (DRIVERS, DRIVER, VEHICLES, VEHICLE, POLICIES, POLICY, STATS, ACTIVITY, RELATIONSHIPS)
  - Zero mock imports anywhere in identity feature. Builds pass.
- **Administration & Security Center API Integration**: Fully API-driven. `api/types.ts` (9 backend-mirror interfaces), `api/mapper.ts` (6 mappers), 2 API services (`admin.api.ts` with getDashboard/getReviews/getEvents/getHealth/getModels/getPerformance + `manual-review.api.ts` with approveReview/rejectReview), updated `endpoints.ts` (approve/reject review endpoints), updated `query-client.ts` (ADMIN keys). `hooks/use-admin-api.ts` (7 React Query hooks with 15s-30s polling + approve/reject mutations). Rewritten `hooks/use-admin.ts` (composite hook, reviews/events/health/models/performance from real API, users/roles/rbac/sessions/notifications/org/score/insights/activity/quick-actions from static constants). `constants.ts` replaces all mock data imports with static typed data. `page.tsx` updated with loading skeleton + error/retry states, SecurityCommandCenter wired into overview tab. `user-profile.tsx` fixed (removed inline MOCK_ACTIVITIES). `SecurityCommandCenter` hackathon (4-view SOC: Operations Wall, Incident Board with drag-and-drop + collaboration notes, Risk Gauge with animated SVG donut, Executive Snapshot with print). Zero mock imports. `tsc --noEmit` silent, `vite build` passes.
- **Recognition Center real-pipeline debugging (Prompt x)**: Root cause of "all stages Completed but no results / nothing reaches backend" was: (1) `models/yolov8n.pt` missing -> detection aborted instantly (YOLO 8.3.87, now downloaded); (2) orchestrator called `.get()` on `DetectionResult` objects (fixed: `d.model_dump()` in `_detect_plates`); (3) `_crop_plates` required 8-value quad bbox but detection produces 4-value `[x1,y1,x2,y2]` -> zero crops (fixed: handle 4 or 8 values); (4) EasyOCR models not cached -> download stalled (now downloaded via curl from GitHub into `~/.EasyOCR/model/`). Live verification: `POST /api/v1/pipeline/process/upload` with `bus.jpg` returns `success:true`, 6 plates detected, 3 recognized (`EMISIONES`/`MUBEE`/`WO`), decision `MANUAL_REVIEW`, gate stays closed; (5) CPU OCR too slow on large crops - added `OCR_MAX_IMAGE_DIM=480` downscale in `PlateReader.read` (cold ~50s, warm ~36s for 6 crops). Frontend: `pipeline.api.ts` rewritten with `normalizePipelineResult` mapping backend `PipelineData` -> `ApiPipelineResult` (stage names from `_`-prefixed backend stages, decision `GRANT/DENY/MANUAL_REVIEW` -> `granted/denied/manual_review`, no longer throws on `success:false`); `page.tsx` `runRecognition` now applies real backend stages to the progress UI via `applyBackendStatus`, shows toasts on failure/error instead of always showing "Completed". `tsc --noEmit` silent, `vite build` passes. Backend is now run without reload (WatchFiles reloader crashes silently on Windows).
- **Operator Console Restructure (current generation focus)**: Gate Operations is now the home/landing page — `index.tsx` `beforeLoad` redirects `/` -> `/gate-operations`; old Dashboard moved to new route `/mission-control` (`src/routes/mission-control.tsx` renders `DashboardPage`). Sidebar reordered (Operations: Gate Operations first, then Mission Control, Live Monitoring, Access Control; Enterprise Identity stays last with "Future" badge). `top-nav.tsx` routeTitles updated (+`/mission-control`). Gate Operations page rebuilt as an operator console: STEP 1 System Health readiness strip (`components/operator-readiness.tsx` — Camera Online / Models Ready / Database Connected / Gate Ready derived live from `useSystemHealth` + `ops.gates` health), STEP 2 humanized `Live Pipeline` (`components/operator-pipeline.tsx` — Vehicle Detected -> Plate Read -> Vehicle Identified -> Driver Verified -> Decision Made -> Gate Opened, driven by `decisionFlow.activeStage`, entry vs exit label sets) + `Vehicles Inside` counter (`components/vehicles-inside.tsx` with oldest-inside detail), then Entry/Exit Control, Gate Status, Active Sessions, Recent Transactions, and Advanced Controls & Diagnostics collapsed. Header has a **Run Demo** button that auto-runs entry -> (7s) -> exit through the real gate-workflow API and shows "Demo Completed Successfully". Identity page now shows a "Future Enterprise Feature" banner. `tsc --noEmit` silent, `vite build` passes.
- **Backend feature flags**: `app/config/settings.py` added `ENABLE_ENTERPRISE_IDENTITY` (default `False`), `ENABLE_ENROLLMENT` (`False`), `ENABLE_MANUAL_REVIEW` (`True`), `ENABLE_ADVANCED_ANALYTICS` (`True`), `ENABLE_RECOGNITION_LAB` (`True`). `app/api/v1/__init__.py` now registers `identity_router` (+ legacy empty `vehicles_router`) ONLY when `ENABLE_ENTERPRISE_IDENTITY=true`, so Mode-B identity routes are hidden from Swagger by default; verified app imports with 98 routes, identity absent when flag off and present when on. Gate/pipeline/dashboard/admin/system routes always registered (they back visible pages; reports/analytics/manual-review endpoints live inside the `/admin` router so they stay on).
- **Backend test remediation (full suite green)**: `pytest tests` now passes **681 tests** (was 14 failures). Root causes fixed: (1) `PlateReader`/`OcrService` switched from per-image `read_from_image`/`extract_text` to a **single batched `read_many(images) -> list[{plate_index, raw_text, cleaned_text, confidence, validation_status, validation_message}]`** API (each crop read once, offsets applied per result; full-image `read()` still works when no crops); orchestrator `_recognize_plates` consumes the batched contract; all gate/session-dispatch/vehicle/ocr/plate-reader tests updated to mock `read_many` with dict entries; (2) `test_process_upload_invalid_file`/`test_process_camera_no_camera` now assert **422** (friendly `ContextValidationError` -> 422 via exception handler) instead of 500; (3) `FrameProcessor` no longer used by `PlateReader._preprocess` (vectorized resize + normalize via `cv2`). Graceful-error tests cover invalid plates, single-read failures, gate DENY with closed gate, and warm-model short-circuit.

### Active
- (none)

### Blocked
- (none)

## Next Move
- **Live Capture flow wired (Prompt 27)**: Recognition Center now has a "Live Capture" button that drives the backend-attached camera: `POST /camera/start` -> `POST /pipeline/process/camera` (full pipeline on a live server frame, `finalize=true`) -> result rendered in the main view -> `POST /camera/stop` in a `finally` (best-effort release). New lean `src/services/api/camera.api.ts` (start/stop/status/detect/capture, backend-mirror `CameraStatus`/`DetectedCamera` types, `ENDPOINTS.CAMERA.DETECT` added); `processPipelineCameraApi` now accepts `cameraId` + `finalize`; new hooks `useProcessPipelineCamera`, `useCameraStatus` (5s poll while running), `useStartCamera`/`useStopCamera` (invalidate `QUERY_KEYS.CAMERA.STATUS`), `useDetectCameras`. Verified: `POST /pipeline/process/camera` returns friendly 422 "Camera is not running" when the camera isn't started (surfaces as a start-failure notification in the UI); tsc silent, vite build passes (entry chunk ~78 kB). Remaining: verify Live Capture in the browser against a machine with a webcam (the backend `/camera/start` opens the server-side camera via OpenCV source 0; role-gated ADMIN/SECURITY_OFFICER); consider gating the Recognition Center "laboratory" behind `ENABLE_RECOGNITION_LAB` in the frontend since the flag exists server-side. Pending user confirmation: `identity-intelligence-panel.tsx` (hardcoded data).

## Relevant Files
- `src/features/identity/`: fully API-integrated (types/api.ts, api/mapper.ts, hooks/, page.tsx, components/, zero mock imports)
- `src/features/recognition/`: fully API-integrated (types/api.ts, api/mapper.ts, hooks/, updated components)
- `src/features/gate-operations/`: fully API-integrated (api/types.ts, api/mapper.ts, hooks/, constants.ts, TrafficPlayback hackathon)
- `src/features/reports/`: fully API-integrated (api/types.ts, api/mapper.ts, hooks/, 19 components, SecurityIntelligenceCenter hackathon)
- Reports data layer (Prompt 23b rewrite): `src/features/reports/api/types.ts` + `api/mapper.ts` (backend-mirror shapes), `src/services/api/reports.api.ts`, `analytics.api.ts`, `event.api.ts` (decision history), `export.api.ts` (blob download) - all consume real backend contracts
- `src/features/administration/`: fully API-integrated (api/types.ts, api/mapper.ts, hooks/, constants.ts, SecurityCommandCenter hackathon, page.tsx with loading/error/retry)
- `src/features/system/`: fully API-integrated (api/types.ts, api/mapper.ts, hooks/, DigitalTwinMonitor hackathon, page.tsx with loading/error/retry)
- `src/services/api/`: auth, recognition (pipeline, recognition), identity (driver, vehicle-profile, policy, enrollment, identity), gate-session, gate-transaction, workflow, reports, export, analytics, admin, manual-review, system, backup, dashboard, event, gate, camera
- `src/lib/api/`: endpoints.ts, query-client.ts, axios.ts, interceptors.ts, errors.ts, api-client.ts
- `gatevision-backend/app/services/ai/orchestrator/orchestrator.py`: fixed `_detect_plates` (`d.model_dump()` on DetectionResult) and `_crop_plates` (4- or 8-value bbox)
- `gatevision-backend/app/services/ai/ocr/ocr_service.py` + `plate_reader.py`: batched `read_many(images)` API (dict entries with `plate_index` offsets)
- `gatevision-backend/app/services/ai/warmup.py` + `gatevision-backend/scripts/`: model warmup script
- `gatevision-backend/app/services/gate/`: `active_session_matcher.py`, `session_gate_service.py`, `session_verification_service.py`
- `gatevision-backend/app/middleware/request_logging.py` + `app/api/v1/pipeline/routes.py`: request-id + per-request timing; orchestrator/camera throttle + dedupe
- `gatevision-backend/scripts/benchmark_pipeline.py`: synthetic dataset + summary reports (`reports/pipeline_benchmark.*`)
- `gatevision-backend/tests/`: test_plate_reader.py, test_ocr_service.py, test_pipeline_api.py, test_pipeline_orchestrator.py, test_pipeline_gate_integration.py, test_pipeline_session_dispatch.py, test_pipeline_vehicle_integration.py, test_session_gate_service.py, test_session_verification.py, test_active_session_matcher.py, test_session_service_extended.py
- `gatevision-backend/models/yolov8n.pt`: plate-detection model (now present)
- `~/.EasyOCR/model/craft_mlt_25k.pth` + `english_g2.pth`: EasyOCR models (now present)
- `gatevision-frontend/src/services/api/pipeline.api.ts`: `normalizePipelineResult` maps backend `PipelineData` -> `ApiPipelineResult`
- `gatevision-frontend/src/features/recognition/page.tsx`: `runRecognition` applies real stages + failure/error toasts
- `gatevision-backend/app/api/v1/recognition/routes.py`: recognition history (`GET /recognition/history`, `DELETE /recognition/history/{record_id}`, `DELETE /recognition/history`), result, models
- `gatevision-frontend/src/components/ui/tabs.tsx`: self-contained `Tabs` (TabsContext) - do not swap for radix tabs
- `gatevision-frontend/vite.config.ts`: `manualChunks` vendor splitting (entry chunk ~78 kB)
